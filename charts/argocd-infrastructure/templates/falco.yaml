{{- if .Values.falco.enabled }}
# ArgoCD Application: Falco
# Sync Wave 0 - Deploy runtime security monitoring (cluster-wide)
# Only deployed if falco.enabled=true

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: falco
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    chart: falco
    repoURL: https://falcosecurity.github.io/charts
    targetRevision: {{ .Values.falco.version | default "9.1.0" }}
    helm:
      releaseName: falco
      valuesObject:
        # Pin the driver explicitly (chart 9.x defaults to `auto` which
        # picks modern_ebpf on 5.8+ kernels — as most managed clusters run —
        # but pinning avoids surprises if the auto-selection heuristic changes).
        # modern_ebpf is CO-RE / no kernel-module build required.
        driver:
          kind: modern_ebpf

        # falcosidekick fans Falco events out to Alertmanager. It POSTs to
        # the alertmanager v2 alerts API (hostport + /api/v2/alerts), so
        # runtime detections show up alongside Prometheus alerts and follow
        # the same receiver routing. minimumpriority=notice drops debug/info
        # chatter. This is the reporting funnel for Falco.
        falcosidekick:
          enabled: true
          config:
            alertmanager:
              hostport: "http://monitoring-kube-prometheus-alertmanager.monitoring:9093"
              minimumpriority: "notice"
              expandlabels: true

  destination:
    server: https://kubernetes.default.svc
    namespace: falco

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        maxDuration: 3m
{{- end }}
