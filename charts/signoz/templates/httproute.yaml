{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "signoz.fullname" . }}
  namespace: {{ include "signoz.namespace" . }}
  labels:
    {{- include "signoz.labels" . | nindent 4 }}
spec:
  parentRefs:
    # group/kind made explicit so the API server's defaulting doesn't show up
    # as a permanent diff in ArgoCD.
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ include "signoz.gateway.name" . }}
      namespace: {{ include "signoz.gateway.namespace" . }}
  hostnames:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - {{ . }}
    {{- end }}
    {{- else }}
    - {{ include "signoz.gateway.hostname" . }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        # SigNoz UI/query service (chart svc name "signoz", http port 8080)
        - group: ""
          kind: Service
          name: signoz
          port: 8080
          weight: 1
{{- end }}
