{{- if and .Values.enabled .Values.externalSecrets .Values.externalSecrets.enabled }}
# Shared EdDSA signing key for the hub's boot-time '*'-grant mint. Exposed to
# the enroll initContainer as CADENCE_ISSUER_KEY.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cadence-secrets
  namespace: {{ include "cadence.namespace" . }}
  labels:
    {{- include "cadence.labels" . | nindent 4 }}
spec:
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStore | default (printf "%s-secretstore" (include "cadence.namespace" .)) }}
    kind: {{ .Values.externalSecrets.secretStoreKind | default "ClusterSecretStore" }}
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  target:
    name: cadence-secrets
    creationPolicy: Owner
  data:
    {{- range .Values.externalSecrets.secrets }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        key: {{ .remoteKey }}
        {{- if .property }}
        property: {{ .property }}
        {{- end }}
    {{- end }}
{{- end }}
