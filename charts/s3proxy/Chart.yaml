apiVersion: v2
name: s3proxy
description: S3 API to object-store translation via jclouds (azureblob, s3, google-cloud-storage, ...) so an S3 client can persist to any supported backend
type: application
version: 1.0.0
appVersion: "3.3.0"

keywords:
  - s3
  - object-storage
  - jclouds
  - proxy
  - external-secrets

sources:
  - https://github.com/gaul/s3proxy

maintainers:
  - name: Monobase
    email: hello@monobase.com

annotations:
  category: Storage
  licenses: Apache-2.0
