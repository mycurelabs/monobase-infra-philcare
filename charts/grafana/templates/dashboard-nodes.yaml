{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboard-nodes
  namespace: {{ include "grafana.namespace" . }}
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Kubernetes"
data:
  nodes.json: |-
{{ .Files.Get "dashboards/dashboard-nodes.json" | indent 4 }}
{{- end }}
