{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboard-apiserver
  namespace: {{ include "grafana.namespace" . }}
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Kubernetes"
data:
  api-server.json: |-
{{ .Files.Get "dashboards/dashboard-apiserver.json" | indent 4 }}
{{- end }}
