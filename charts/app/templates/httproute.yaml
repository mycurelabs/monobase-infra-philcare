{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "app.fullname" . }}
  namespace: {{ include "app.namespace" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - name: {{ include "app.gateway.name" $ }}
      namespace: {{ include "app.gateway.namespace" $ }}
      sectionName: {{ .sectionName }}
    {{- end }}
    {{- else }}
    - name: {{ include "app.gateway.name" . }}
      namespace: {{ include "app.gateway.namespace" . }}
      {{- if .Values.gateway.sectionName }}
      sectionName: {{ .Values.gateway.sectionName }}
      {{- else }}
      sectionName: https  # Default: attach to HTTPS listener
      {{- end }}
    {{- end }}
  hostnames:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - {{ .hostname }}
    {{- end }}
    {{- else }}
    - {{ include "app.gateway.hostname" . }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "app.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
