# Default values for external-secrets-stores
# This chart creates ClusterSecretStore resources for External Secrets Operator

# ClusterSecretStore configurations
# Supports multiple providers: gcp, aws, azure
secretStoreName: gcp-secretstore

# Cluster-scoped ExternalSecrets are OPT-IN (a tenant that doesn't use cloudflare
# DNS or velero shouldn't sync those secrets into namespaces it lacks). Enable
# per-cluster in values; default off = store-only.
cloudflare:
  enabled: false   # renders the cert-manager + external-dns cloudflare-api-token ExternalSecrets
velero:
  enabled: false   # renders the velero-credentials + velero-repo-credentials ExternalSecrets

cloudflareSecretName: cloudflare-api-token
veleroObjectStoreSecret: velero-object-store-credentials
stores: []  # Example GCP Secret Manager store:
  # - name: gcp-secretstore
  #   provider: gcp
  #   gcp:
  #     projectId: "my-gcp-project"
  #     auth:
  #       # Option 1: Workload Identity (recommended for GKE)
  #       workloadIdentity:
  #         enabled: true
  #         clusterLocation: us-central1
  #         clusterName: my-cluster
  #         serviceAccountRef:
  #           name: external-secrets
  #           namespace: external-secrets-system
  #       # Option 2: Service Account Key
  #       serviceAccountKey:
  #         enabled: false
  #         secretRef:
  #           name: gcpsm-secret
  #           key: secret-access-credentials
  #           namespace: external-secrets-system

  # Example AWS Secrets Manager store:
  # - name: aws-secretstore
  #   provider: aws
  #   aws:
  #     region: us-east-1
  #     service: SecretsManager
  #     auth:
  #       jwt:
  #         serviceAccountRef:
  #           name: external-secrets
  #           namespace: external-secrets-system

  # Example Azure Key Vault store:
  # - name: azure-secretstore
  #   provider: azure
  #   azure:
  #     vaultUrl: "https://my-vault.vault.azure.net"
  #     tenantId: "tenant-id"
  #     auth:
  #       workloadIdentity:
  #         enabled: true
  #         serviceAccountRef:
  #           name: external-secrets
  #           namespace: external-secrets-system
