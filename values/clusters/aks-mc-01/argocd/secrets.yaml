# Infrastructure-scoped ExternalSecrets for AKS-MC-01 (PhilCare).
# Empty: external-dns and the Tailscale operator are disabled on this cluster, so
# there are no cluster-wide infrastructure-* secrets to sync. Per-app/DB secrets
# live in the deployment overlays (philcare-<env>-*) and the database-secrets chart.
secrets: []
